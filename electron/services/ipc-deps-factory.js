function buildMainIpcDeps({
  ipcMain,
  screenManager,
  createProjectorWindow,
  projectorRecoveryBridge,
  projectorControlBridge,
  getControlWindow,
  getProjectorWindow,
  appendBgDebug,
  projectorSceneState,
  resolveYouTubeStream,
  sanitizeMediaFileName,
  mediaState,
  downloadService,
  ytdlpService,
  verifyLicenseToken,
  getDeviceId,
  licenseRuntime,
  trialGuard,
  readLegalDocument,
  dialog,
  resolveAbsolutePath,
  normalizeForCompare,
  isPathWithinRoot,
  pptConvertTimeoutMs,
  resolveRuntimePptConvertScriptPath,
  app,
  electronDir,
  processResourcesPath,
  formatBackupStamp,
  collectReferencedMediaPathsFromQueue,
  copyDirectoryMerge,
}) {
  return {
    ipcMain,
    screenManager,
    createProjectorWindow,
    forceCloseProjectorWindow: projectorRecoveryBridge.forceCloseProjectorWindow,
    requestCloseControlWindow: projectorControlBridge.requestCloseControlWindow,
    getControlWindow,
    getProjectorWindow,
    appendBgDebug,
    sendToProjectorShell: projectorControlBridge.sendToProjectorShell,
    openYouTubeWatchInProjector: projectorControlBridge.openYouTubeWatchInProjector,
    projectorSceneState,
    resolveYouTubeStream,
    sanitizeMediaFileName,
    mediaYouTubeCacheDir: mediaState.getMediaYouTubeCacheDir(),
    downloadService,
    ytdlpService,
    verifyLicenseToken,
    getDeviceId,
    licenseRuntime,
    trialGuard,
    readLegalDocument,
    dialog,
    resolveAbsolutePath,
    normalizeForCompare,
    isPathWithinRoot,
    mediaDir: mediaState.getMediaDir(),
    mediaImagesDir: mediaState.getMediaImagesDir(),
    mediaVideosDir: mediaState.getMediaVideosDir(),
    mediaPdfDir: mediaState.getMediaPdfDir(),
    mediaPptDir: mediaState.getMediaPptDir(),
    pptConvertTimeoutMs,
    resolveRuntimePptConvertScriptPath,
    app,
    electronDir,
    processResourcesPath,
    formatBackupStamp,
    collectReferencedMediaPathsFromQueue,
    copyDirectoryMerge,
  };
}

module.exports = {
  buildMainIpcDeps,
};
